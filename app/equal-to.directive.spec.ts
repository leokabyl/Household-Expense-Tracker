import { EqualToDirective } from './equal-to.directive';

describe('EqualToDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualToDirective();
    expect(directive).toBeTruthy();
  });
});
